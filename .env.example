# Provider: openai | anthropic | none (deterministic fallbacks only)
LLM_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Optional: skip LLM even if keys are set
FORCE_OFFLINE=false

DATA_DIR=data/personas
